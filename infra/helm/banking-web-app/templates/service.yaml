apiVersion: v1
kind: Service
metadata:
  name: {{ include "banking-web-app.fullname" . }}
  labels:
    {{- include "banking-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "banking-web-app.selectorLabels" . | nindent 4 }}
